using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
The colors are back in some way. Slow existing life. Just enjoying small things.
Just trying to live.

Forest Steps
Bridge
Bench at the Lake
Past Self Show
    Mock player with the written text. World is gray around the past.
Temple
Garden

*/
public class LevelScript_04 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
